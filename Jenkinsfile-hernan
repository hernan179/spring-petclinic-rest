pipeline {
    agent any
    tools {
        maven 'maven3.8.8'
    }

    stages {
        stage('Build'){
            steps {
                echo 'Witout Checkout form jenkingsfile-hernan, implicited was done';
                //git 'https://github.com/hernan179/spring-petclinic-rest.git'
                 sh 'mvn clean compile -B -ntp'
            }
        }
        stage('Testing'){
            steps {
                echo 'Testing';
                sh 'mvn test -B -ntp'
                
            }

        }
        stage('Sonar'){
            steps {
                echo 'Qualiting';
            }
        }
        stage('Artifactory'){
            steps {
                echo 'Artifactory...saving';
            }
        }
        stage('Deploy'){
            steps {
                echo 'Deploying... on jboss';
            }
        }
    }
}
